{
  "name": "social-poller",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "engines": {"node": ">=22.18.0"},
  "scripts": {
    "parse:page": "node tools/parse-page.mjs",
    "import:export": "node tools/import-export.mjs",
    "dev": "wrangler dev",
    "types": "wrangler types",
    "check": "wrangler types && wrangler types -c wrangler.stratus.jsonc --env-interface StratusEnv stratus-configuration.d.ts && tsc --noEmit",
    "dev:stratus": "wrangler dev -c wrangler.stratus.jsonc",
    "build:stratus": "wrangler deploy -c wrangler.stratus.jsonc --dry-run",
    "deploy:stratus": "wrangler deploy -c wrangler.stratus.jsonc",
    "test": "node --test tests/*.test.ts",
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {"wrangler": "4.127.1", "typescript": "~5.9.3", "@types/node": "^22.18.0"}
}
